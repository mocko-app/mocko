apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mocko.fullname" . }}-test-connection"
  labels:
    {{- include "mocko.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mocko.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
