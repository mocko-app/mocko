apiVersion: v1
kind: Service
metadata:
  name: {{ include "mocko.fullname" . }}
  labels:
    {{- include "mocko.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mocko.selectorLabels" . | nindent 4 }}
